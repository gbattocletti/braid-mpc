# Scripts

Scripts are provided to expose functions that allow to run the simulations with the different braid controllers:
- `d_bmpc.py`: the proposed approach with distributed MPC
- `c_dmpc.py`: a centralized version of the proposed approach
- `gbc.py`: grid braid controller (baseline)